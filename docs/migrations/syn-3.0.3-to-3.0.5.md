# Migration: syn 3.0.3 -> 3.0.5

Generated by `cargo xtask bump --source syn 3.0.5`. This file is the record of what the
migration did mechanically and what it left for a human.

## Provenance

| | |
|---|---|
| archive | `syn-3.0.5.crate` |
| role | `glossary` — quoted, never claimed |
| crate sha256 | `12df2e0110f65b775f769bb17ef989067a1d931b2eb822bd4346631eeada89f9` |
| previous crate sha256 | `53e9bae58849f64dfa4f5d5ae372c8341f7305f82a3868709269343628b659a3` |
| previous src_tree_sha256 | `d1594f4c97daa8d78edb2c54fa26ecd1deb51317823d5828bc84639674841ad0` |

## Files

| file | lines | shifted | edited | dropped |
|---|---:|---:|---:|---:|
| `src/item.rs` | 3922 → 3923 | 0 | 0 | 0 |
| `src/lit.rs` | 1880 → 1881 | 0 | 0 | 0 |

## Records to review

None: every surviving record covers the same text it did before.

## Records dropped

None.

## Checklist

- [x] `cargo update -p syn@3.0.3 --precise 3.0.5`
- [x] `cargo test -p expand -- --ignored` — the expander is built from this source, so its transcripts move with it. Read the diff: it is the generated code the site shows.
- [x] `cargo xtask coverage` is green
- [x] `cargo test --workspace` is green (examples still produce their transcripts)
- [x] every record in *Records to review* has been read against the new source
- [x] prose outside the store that names the version (PLAN.md, README.md, docs/) is current

## What it came to

Nothing moved. Three files changed upstream and two of them grew by a line:
`lit.rs:249` and `item.rs:2282` each became two lines, and `lib.rs:367` is the
`html_root_url` string. The glossary's two `lit.rs` entries quote lines 23-62
and 64-69, both above the insertion, so every one of the 50 `syn` quotations
covers the same text it covered before.

`expected.txt` did not move either: the expander built from `syn` 3.0.5 emits
the six committed expansions byte for byte, and the 30 narrative steps that
quote emitted code still match a real expansion (D10). So this bump changes
what the repo is *pinned* to and nothing a reader sees — which is what a patch
release of a parser should look like, and worth having proved rather than
assumed.

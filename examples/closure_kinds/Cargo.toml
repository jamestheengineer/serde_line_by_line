[package]
name = "closure_kinds"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

# No dependencies on purpose. serde_core writes essentially no closures, so
# this half of unit 14 is written from scratch and stands alone.
[dependencies]

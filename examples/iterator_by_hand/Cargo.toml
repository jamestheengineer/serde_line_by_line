[package]
name = "iterator_by_hand"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

# No dependencies on purpose. Unit 14 is mostly supplementary — serde_core
# consumes iterators carefully in three places and teaches the protocol
# nowhere — so nothing here should need serde_core to run.
[dependencies]

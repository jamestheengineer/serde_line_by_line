[package]
name = "lifetime_basics"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

# No dependencies on purpose. This unit is supplementary — it teaches the
# language, not the crate — so nothing here should need serde_core to run.
[dependencies]
